describe('Sample Unit Test - UT_nama_nim', () => {
  it('should pass a basic assertion', () => {
    expect(1 + 1).toBe(2);
  });

  it('should handle string operations', () => {
    const greeting = 'MoodFlix';
    expect(greeting.toLowerCase()).toBe('moodflix');
  });
});
